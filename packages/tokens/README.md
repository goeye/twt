# Tokens Package

This package contains design tokens shared by web and mobile implementations.

## Rules

1. Keep semantic names stable.
2. Do not use raw color/spacing values directly in components.
3. Add new tokens only when existing semantic roles are insufficient.

## Files

- `tokens.example.json`: starter token contract.

