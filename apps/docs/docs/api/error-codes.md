# 错误码参考

所有 API 遵循统一的响应格式。

## 响应格式

```json
{
  "code": 1,
  "msg": "ok",
  "data": ""
}
```

## 业务状态码

| code | 说明 |
| --- | --- |
| `1` | 请求成功 |
| `-1` | 请求失败，具体原因见 `msg` 字段 |

::: tip 提示
当 `code` 为 `-1` 时，`msg` 字段会返回具体的错误描述信息。
:::
