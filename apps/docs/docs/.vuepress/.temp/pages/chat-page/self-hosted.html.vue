<template><div><h1 id="自托管部署" tabindex="-1">自托管部署</h1>
<p>下载包含聊天页面代码的 HTML 文件，上传到你自己的服务器，通过自有域名访问。</p>
<h2 id="部署步骤" tabindex="-1">部署步骤</h2>
<h3 id="_1-下载-html-文件" tabindex="-1">1. 下载 HTML 文件</h3>
<p>登录 TWT Chat 后台，进入 <strong>设置 &gt; 聊天页面</strong>，点击「下载 HTML 文件」。</p>
<p>下载的文件已包含你的应用配置（appid 等），无需额外修改即可使用。</p>
<h3 id="_2-上传至服务器" tabindex="-1">2. 上传至服务器</h3>
<p>将 HTML 文件上传到你的 Web 服务器，确保可通过 HTTP/HTTPS 访问。</p>
<h3 id="_3-配置域名-可选" tabindex="-1">3. 配置域名（可选）</h3>
<p>将你的自有域名解析到服务器，访客即可通过自有域名打开聊天窗口。</p>
<div class="hint-container tip">
<p class="hint-container-title">提示</p>
<p>建议使用 HTTPS 部署，确保聊天数据传输安全。</p>
</div>
<h2 id="自定义配置" tabindex="-1">自定义配置</h2>
<p>自托管页面同样支持通过 URL 参数自定义语言和传递客户信息，详见 <a href="./parameters">URL 参数说明</a>。</p>
</div></template>


