# 生成 PRD 文档

根据源码自动生成产品需求文档（PRD），保存至 `docs/` 目录。

调用方式：`/prd 功能名称`（如 `/prd 角色权限管理`、`/prd 访客标签`）

参数：$ARGUMENTS

## 步骤

### 第零阶段：读取历史需求决策（不可跳过）

0. 读取 `memory/` 目录下与该功能相关的 `decisions-*.md` 决策记录文件。这些文件记录了用户在历次会话中确认的需求结论，**优先级高于源码中的实现**（因为源码可能尚未更新）。若决策记录与源码冲突，以决策记录为准，并在 PRD 的「待实现功能清单」中标注差异。

### 第一阶段：定位源码

1. 从参数中获取功能名称（即 `$ARGUMENTS`）。若参数为空，询问用户要为哪个功能生成 PRD
2. 运行 `git log --oneline -30` 和 `git diff --stat HEAD~10` 浏览近期改动，定位与该功能相关的源码文件
3. 使用 Glob / Grep 搜索 `apps/web-agent/src/` 和 `packages/ui-agent/src/` 下与功能名称相关的 Vue/TS 文件

### 第二阶段：深度阅读（核心页面 + 关联页面）

4. 逐一阅读**核心功能页面**的文件，提取业务逻辑：
   - 数据结构（接口定义、mock 数据、枚举值）
   - 操作流程（CRUD 入口、路由跳转、弹窗触发）
   - 校验规则（表单验证、maxlength、必填项、按钮禁用条件）
   - 文案字符串（弹窗标题、描述文本、Toast 提示语）
   - 保存方式（按钮提交 vs 失焦保存 vs 实时保存）

5. **全局影响面扫描**（关键步骤，不可跳过）：
   用 Grep 搜索核心实体名称（如角色ID、类型枚举、API 路径等）在**整个项目**中的引用，找出所有关联页面。必须逐一检查：
   - 该功能涉及的**其他页面/模块**的改动（如：角色功能 → 客服列表、客服详情、邀请弹窗、个人资料页面等）
   - 不同**用户视角**下的差异行为（如：管理员看到 vs 普通客服看到；查看自己 vs 查看他人）
   - **列表页面**中该实体的所有状态标签、tag 徽章、操作菜单差异
   - 路由守卫、权限校验、403 页面等**权限拦截点**

### 第三阶段：补充排查清单

6. 在写 PRD 之前，对照以下清单逐项排查，确认是否遗漏：

   **A. 实体生命周期**：
   - 该功能涉及的核心实体有哪些**状态**？（如：待激活、已激活、已禁用、已删除）
   - 每个状态下可执行什么操作？不可执行什么操作？
   - 状态之间如何流转？有无不可逆操作？

   **B. 资源约束与限制**：
   - 是否涉及**配额/名额**？（如：坐席数、角色上限）
   - 不同状态的实体是否占用配额？（如：未激活客服是否占坐席）
   - 是否有**频率限制/冷却时间**？（如：重新邀请间隔）
   - 是否有**数量上限**？（如：最多创建 N 个自定义角色）

   **C. 关联页面改动**：
   - 该功能改动是否导致**其他页面**的 UI/交互发生变化？
   - 列表中的标签、徽章、操作菜单是否需要更新？
   - 详情页/编辑页的字段、按钮、保存方式是否有变更？
   - 不同角色/身份的用户看到的页面是否不同？

   **D. 权限正反面行为**：
   - 如果功能涉及权限，每个权限项**具备时能做什么**？
   - **不具备时表现是什么**？（隐藏入口？按钮置灰？403？）

   **E. 外部触点**：
   - 该功能是否需要在**官网/营销页面**中更新权益说明？
   - 是否需要在**定价/套餐对比**中体现差异？
   - 是否需要更新**帮助文档/用户引导**？

### 第四阶段：撰写文档

7. 按下方「PRD 文档大纲」撰写完整文档
8. 保存至 `docs/PRD-{功能名称}.md`
9. 告知用户文档已生成，给出文件路径

## PRD 文档大纲

生成的文档须包含以下章节（可根据功能复杂度合理增减小节）：

```
# PRD：{功能名称}

> **版本**：v1.0 · {YYYY-MM-DD}
> **状态**：草稿 / 评审中 / 已确认 / 已交付
> **模块编号**：[如 Module 01]

---

## 1. 概述

### 1.1 背景与动机
（用「痛点 → 影响」的方式说明为什么要做这个功能）

| 痛点 | 影响 |
|------|------|
| [用户面临的具体问题] | [该问题导致的后果] |

（表格下方用 1-2 段文字概括产品定位和核心价值）

### 1.2 目标
（用可量化的 Key Result 表达目标，拒绝空泛描述）

| Key Result | 量化标准 |
|-----------|---------|
| KR1：[目标维度] | [可衡量的指标] |

### 1.3 非目标（本期不做）
（明确列出容易被误认为属于本期范围的功能，划清边界。若无则省略）

---

## 2. 用户故事
（格式：「作为 [角色]，我希望 [功能]，以便 [价值]」。每条故事必须包含验收标准）

| ID | 角色 | 用户故事 | 验收标准 | 优先级 |
|----|------|---------|----------|--------|
| US-01 | [角色] | 我希望 [功能描述] | [具体可验证的标准] | P0 |

---

## 3. 功能设计

### 3.1 信息架构
（用 Mermaid 图描述功能模块的层级关系和导航结构）

### 3.2 核心流程
（用 Mermaid 流程图描述主流程：Happy Path + 关键分支 + 异常路径）

### 3.3 子功能详述
（PRD 的核心章节。对每个子功能逐一按以下结构展开。如果某字段确实无内容，填写「无」，不得删除字段）

#### 3.3.X [子功能名称]

**功能描述**：用 1 句话说明该子功能做什么。

**用户场景**：在什么情况下、想要解决什么问题。

**前置条件**：
1. [触发该功能前必须满足的条件]

**交互流程**：
1. 用户执行某操作（如发起、填写、提交、筛选等）
2. 系统进行校验或处理（如字段校验、规则判断、状态更新等）
3. 系统保存结果或更新数据
4. 页面或状态变化（如跳转页面、弹出提示等）

（如包含审批、状态流转或多角色参与，请清晰描述各角色的动作和状态变化路径）

**需求描述（功能规则）**：

1. **输入规则**：各字段的必填与选填规则、格式要求、默认值和可选范围
2. **校验规则**：校验逻辑、不符合规则时的提示文案
3. **业务规则**：数据生成/变更/展示/归档的规则、状态及其含义、状态间的转换条件、排序/筛选/分页/搜索等规则
4. **异常处理**：常见异常场景下系统应如何响应

**后置条件**：
1. 数据新增、修改或删除的结果
2. 状态变化
3. 页面跳转或刷新行为
4. 关联影响（如库存扣减、日志记录、通知发送等）

### 3.4 状态机（如适用）
（仅当功能涉及多状态流转时填写，否则省略。包含 Mermaid 状态图和状态说明表格）

| 状态 | 含义 | 允许的操作 |
|------|------|-----------|

---

## 4. 数据模型
（定义核心业务实体的字段）

| 实体名 | 字段 | 类型 | 说明 |
|--------|------|------|------|

---

## 5. 权限与角色
（列出功能对不同角色的可用性。无权限时的降级表现必须明确）

| 功能 | [角色A] | [角色B] | 无权限时的表现 |
|------|---------|---------|--------------|

---

## 6. 约束与依赖
（列出影响功能实现的约束条件和外部依赖。若无则省略）

| 约束/依赖 | 说明 | 影响范围 |
|----------|------|---------|

---

## 7. 异常处理
（汇总全模块的异常场景）

| 异常场景 | 处理方式 | 用户感知 |
|---------|---------|---------|

---

## 8. 跨模块联动
（描述本模块与其他模块的交互关系。若无则省略）

| 联动模块 | 联动方式 | 说明 |
|----------|----------|------|

---

## 9. 开放问题
（列出当前仍需确认的事项，供评审时讨论。若无则省略）

| # | 问题 | 备选方案 | 当前倾向 | 状态 |
|---|------|---------|---------|------|
```

## 注意事项

- 全文使用**中文**撰写
- 只描述业务逻辑和测试边界，**不涉及**代码实现、技术架构、CSS/UI 结构、组件命名
- **严禁出现英文代码术语**：不得出现变量名（如 accountEnabled、memberCount）、英文状态名（如 blur-to-save）、代码注释风格描述。一律用中文业务语言描述，例如「账号启用状态」而非「accountEnabled: true」
- 所有数据从源码中提取（接口数据结构、mock 数据、校验规则、弹窗文案等），不凭空编造
- **避免冗余重复**：同一条规则只在最合适的章节描述一次，不在多个章节反复出现。如果需要引用已描述的规则，用「详见 x.x 节」指引
- 行文风格和表格格式参考 PRD 模板的结构化格式
- 如果功能尚未完整实现，在概述中标注「状态：部分实现」并说明缺失部分
- 章节若无相关内容可省略，但不要填充无意义的占位文字
- **写完后必须回顾第三阶段的排查清单（A-E），逐项确认无遗漏后再输出**

## 需求决策记录规则

在日常会话中（不仅限于 /prd），遵循以下记录规则：

1. **自动记录**：每次会话涉及需求讨论或代码改动时，将关键结论写入 `memory/decisions-{模块名}.md`
2. **主动记录**：用户说「记一下」「记录」等类似指令时，立刻写入
3. **冲突标注**：如果新结论覆盖了旧决策，在记录中标注「覆盖：原结论是 xxx」
4. **格式**：每条记录为 `- {日期}：{结论}`，按功能分组
